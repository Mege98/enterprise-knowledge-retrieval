# 安全策略

请通过 GitHub Private vulnerability reporting 报告漏洞，不要在公开 Issue 中提交真实 API Key、知识库内容、文件路径、对话记录或其他敏感数据。

## 数据边界

- 精确检索、索引和历史会话默认保存在本机。
- RAG 问答会向用户配置的模型服务发送当前问题和必要的检索片段。
- API Key 可仅通过环境变量提供；选择“保存到本机”时，使用者应保护本机配置目录。
- 发布包和 Issue 附件不得包含索引数据库、设置、日志或真实业务文档。

如果密钥已经进入 Git 历史，应立即吊销和轮换；仅删除当前文件不足以消除泄露风险。
